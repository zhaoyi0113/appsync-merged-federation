getUserHandler:
  handler: subgraph-user/src/index.getUser
  name: ${self:provider.stackName}-getUser
  timeout: 30
  tracing: true

getUserPurchasesDateTimeHandler:
  handler: subgraph-user/src/index.getUserPurchasesDateTimeHandler
  name: ${self:provider.stackName}-getUserPurchasesDateTime
  timeout: 30
  tracing: true