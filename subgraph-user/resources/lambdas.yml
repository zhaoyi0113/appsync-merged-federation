getUserHandler:
  handler: subgraph-user/src/index.getUser
  name: ${self:provider.stackName}-getUser
  timeout: 30
  tracing: true

getUserPurchasesHandler:
  handler: subgraph-user/src/index.getUserPurchases
  name: ${self:provider.stackName}-getUserPurchases
  timeout: 30
  tracing: true