getProductHandler:
  handler: subgraph-product/src/index.getProduct
  name: ${self:provider.stackName}-getProduct
  tracing: true

getPurchasesHandler:
  handler: subgraph-product/src/index.getPurchases
  name: ${self:provider.stackName}-getPurchases
  tracing: true

publishProductUpdatedEventHandler:
  handler: subgraph-product/src/index.publishProductUpdatedEvent
  name: ${self:provider.stackName}-publishProductUpdatedEvent
  tracing: true